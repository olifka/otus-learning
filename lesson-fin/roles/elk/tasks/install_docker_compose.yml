---
- name: Download docker-compose script
  get_url:
    url: "https://github.com/docker/compose/releases/download/{{ docker_compose_ver }}/docker-compose-$(uname -s)-$(uname -m)"
    dest: /usr/sbin/docker-compose
    owner: root
    group: root
    mode: u=rwx,g=rx,o=rx
...